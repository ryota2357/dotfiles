{
  "altScreenMode": "always",
  "colorScheme": "dark",
  "model": "Gemini 3.6 Flash (High)",
  "notifications": true,
  "permissions": {
    "allow": [
      "command(gh issue list)",
      "command(gh issue status)",
      "command(gh issue view)",
      "command(gh pr diff)",
      "command(gh pr list)",
      "command(gh pr status)",
      "command(gh pr view)",
      "command(gh release list)",
      "command(gh release view)",
      "command(gh run list)",
      "command(gh run status)",
      "command(gh run view)",
      "command(gh run watch)",
      "command(git blame)",
      "command(git diff)",
      "command(git log)",
      "command(git ls-files)",
      "command(git ls-remote)",
      "command(git ls-tree)",
      "command(git show)",
      "command(git status)",
      "command(grep)",
      "command(ls)",
      "command(pwd)",
      "command(rg)",
      "read_file(/Users/ryota2357/ghq/github.com/ryota2357)"
    ],
    "deny": [
      "command(sudo)"
    ],
    "trustedWorkspaces": [
      "/Users/ryota2357/ghq/github.com/ryota2357/"
    ]
  }
}
